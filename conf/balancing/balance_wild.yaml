treats_wide: "${compute.processed}/wide_treatments_gee/wide_treats_mtbs_wildlands.feather"
dict_paths:
  frp: ${compute.processed}/frp/frp_wide.feather
  disturbances: ${compute.processed}/disturbances/disturbances_wide.feather
  dem: ${compute.processed}/dem/dem.feather
  land_type: ${compute.processed}/land_type/land_type.feather
  prism: ${compute.processed}/prism/prism_processed_wide.feather
  vegetation: ${compute.processed}/vegetation/frac_vegetation_wide.feather
treatment: dnbr
land_type: 2
intensity_class: 2
focal_year: 2008
row_id: "grid_id"
intercept: true
save_path: "./new_models_wildlands"