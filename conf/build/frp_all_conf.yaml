template: "${template}"
treat_type: frp
frp: "${save_path}/frp_all_conf/frp_concat.feather"
treatments_path: "${save_path}/treatments_mtbs.feather"
query: "Incid_Type != 'Prescribed Fire'"
save_path: "${save_path}/wide_treatments_frp_all_conf"
lhs:
  frp:
    path: ${save_path}/frp_all_conf/frp_concat.feather
    classify: true
  dnbr: 
    path: ${save_path}/dnbr_gee_inmediate/dnbr_long.feather
    classify: true